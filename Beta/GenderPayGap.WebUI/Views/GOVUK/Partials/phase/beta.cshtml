@using Extensions
@if (!Request.Url.PathAndQuery.EqualsI(@Url.Action("SendFeedback", "Home")))
{
    <p style="margin-top: 10px">
        <strong class="phase-tag">BETA</strong>
        <span>This is a new service – your <a href="@Url.Action("SendFeedback", "Home")" rel="external" target="_blank" class="font16-ext-link">feedback<span class="visuallyhidden"></span></a> will help us to improve it.</span>
    </p>
}
